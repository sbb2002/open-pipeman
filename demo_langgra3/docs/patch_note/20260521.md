# TODO

* exec() 샌드박스 격리 — 보안 위험 제거 [즉시개선필요]
* node_human_review를 실제 중단점(Interrupt)으로 구현 [미구현]
* retry_count를 intent / exec 별로 분리 [버그]
* cleanup 노드를 AST 정적 분석으로 교체 + 실행 검증 강화 [개선]

참고: demo_langgra3\docs\patch_note\ref\backend-review.html

# Session 1. 동료평가 개선사항 P2 구현 - 인간검증 배지 및 팝업

| 파일명 | 버전 | 내용 |
|--------|------|------|
| `backend.py` | v1 | `node_human_review`를 pass-through에서 LangGraph `interrupt()` 기반 실제 HITL 노드로 교체. `hr_action` 필드(`CellState`) 추가, `edge_after_human_review` 조건부 엣지 추가(승인→wrap / 재작성→generate). `_stream_run`·`_stream_resume` 모두 `astream_events` → `astream(stream_mode="updates")` + `__interrupt__` 감지 방식으로 전환. `Command(resume={"action":...})` 로 재개 시 빈 딕셔너리 재트리거 버그 수정. `/cell/resume` 엔드포인트에 `_stream_resume` 제너레이터 분리 |
| `css/cell.css` | v1 | Human Review 대기 상태 스타일 추가: `.cell-review-badge`(우상단 `!` 뱃지, 펄스 애니메이션), `.review-pending`(빨간 테두리 강조) |
| `css/overlays.css` | v1 | Human Review 팝업 전체 스타일 추가: `#human-review-modal` 레이아웃, 신뢰도 바(`.hr-conf-track`/`#hr-conf-fill`), 항목 태그(`.hr-tag-ok/warn/err/info`), 리스크(`.hr-risk`), 코드 미리보기(`#hr-code-preview`), 액션 버튼(`.hr-btn-approve/modify/reject`) |
| `index.html` | v1 | Human Review 팝업 스켈레톤 추가: `#human-review-overlay` — 헤더(셀 라벨·뱃지), 본문(AI 요약·신뢰도 바·항목별 검토·리스크·코드 미리보기·재작성 입력란), 액션 버튼(승인·재작성 요청·거절) |
| `js/pipeline.js` | v1 | Human Review HITL 기능 전면 추가. `showReviewBadge()`/`removeReviewBadge()`: 셀에 `!` 뱃지 부착·제거. `showHumanReviewPopup()`/`_fillHumanReview()`/`_setConfBar()`: 팝업 렌더링 및 분석 데이터 채우기. 승인·재작성 요청·거절 버튼 로직 구현. `resumeCell()` 재작성: `paused` 재진입 시 팝업 재표시, `result` 수신 시 `node.result` 저장. `runFromNode()`에서 `paused` 발생 시 잔여 실행 순서를 `state.pendingRunOrder`에 저장하고 `resumeCell` 완료 후 이어서 실행하는 연속 실행 로직 추가. 기존 `showPausedPanel()` 제거 |