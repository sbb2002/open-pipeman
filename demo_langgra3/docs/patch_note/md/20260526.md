# Session 1. Cell config 웹 검색 기능 동작 원리

| 파일명 | 버전 (수정 전) | 내용 |
|---|---|---|
| `api.py` | v1 | `RunCellRequest`에 `allow_cleanup`, `force_review`, `hr_strictness`, `allow_web_search`, `max_retries` 필드 추가. `initial_state`에 신규 필드 초기화. `NODE_NAMES`에 `search_tools` 추가. `e2b_api_key`를 서버 env에서 직접 주입으로 변경 |
| `css/settings.css` | v2 | `.retry-slider-row` 제거. `.retry-labels-row`, `.retry-label`, `.retry-input-row`, `.retry-number-input` 추가 (5단계 quantized 슬라이더 레이아웃) |
| `graph.py` | v1 | `search_tools` 노드 등록. `inject_schema` 이후 `edge_after_inject` 조건부 엣지 연결 (`allow_web_search` 분기) |
| `index.html` | v3 | Strictness 탭 CODE REVIEW 섹션: 슬라이더를 `min=0 max=4` 5단계 quantized로 교체. 각 단계 라벨(3/5/10/20/No limit) 균등 배치. 직접 입력 텍스트박스 추가 |
| `js/runner.js` | v2 | `runCell()` body에 `allow_web_search: node.webSearch`, `max_retries: window.getMaxRetries()` 추가. 불필요한 `e2b_api_key` 제거. `node.allowWebSearch` → `node.webSearch` 오타 수정 |
| `js/settings.js` | v2 | `getMaxRetries()` 노출. 5단계 quantized 매핑 (`_RETRY_STEPS = [3,5,10,20,0]`). 슬라이더 ↔ 텍스트박스 양방향 동기화 (`_syncRetrySliderToInput`, `_syncRetryInputToSlider`, `_retrySnapToStep`) |
| `nodes.py` | v1 | `CellState`에 `max_retries`, `allow_web_search`, `tool_hint`, `license_review` 추가. `node_search_tools` 신규 (PyPI/HF API 병렬 조회 + permissive 라이선스 검증). `edge_after_inject` 신규. `edge_after_classify`에 `license_review` HR 트리거 추가. `node_human_review`에 라이선스 경고 메시지 분기. `edge_after_intent`/`edge_after_execute`에서 `MAX_RETRIES` 상수 → `state["max_retries"]` 동적 참조. `node_generate`에 `tool_hint` 컨텍스트 주입. `search_tools` 로그 3단계 세분화 (candidates / confirmed / non-permissive) |
| `structure.md` | v1 | 노드 실행 순서 플로우 다이어그램 추가. CellState 주요 필드 표 추가. UI 변경 이력 업데이트. 백엔드 API 엔드포인트 표 추가 |

## 기타

* MCP로 작업 시 500+줄 이상의 큰 파일은 모두 타임아웃되는 경향이 있음. 이를 방지하기 위해 너무 클 경우 present_files로 나타내도록 변경함.