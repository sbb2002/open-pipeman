# Session 1. 파일 분할 기준 판단
* js 파일 중 일부 큰 파일을 분리.
분리 전에 의존성 방향을 먼저 정확히 파악하자.의존성 지도가 완성됐다. 작업 전에 핵심 제약을 정리하고 간다.

| 원본 | 분리 후 | 줄 수 변화 |
|---|---|---|
| `pipeline.js` 1,348줄 | `store.js` 81 + `project-ui.js` 694 + `runner.js` 252 + `hitl.js` 304 | —17줄 (중복 제거) |
| `backend.py` 759줄 | `nodes.py` 408 + `graph.py` 62 + `api.py` 224 | —65줄 (`_stream_run`/`_stream_resume` 통합) |


# Session 2. 셀 실행 시 거치는 노드 기능 정리

# 세션 작업 내역 요약

| 파일명 | 버전 | 내용 |
|--------|------|------|
| css/settings.css | v1 | 설정창 가독성 개선: `settings-section-label` 9px→11px, `apikey-hint` 10px→11px, 각 텍스트 색상 `text3`→`text2`/`text`, `#btn-settings` 색상 `text2`→`text`, `field-group label` 스타일 추가 |
| graph.py | v1 | `build_graph(allow_cleanup)` 파라미터 추가로 cleanup 노드 조건부 연결. `node_classify_level` 노드 및 `edge_after_classify` 추가. `execute → classify_level → (human_review \| wrap)` 흐름으로 재구성 |
| index.html | v1 | CELL CONFIG에 `f-cleanup`(Allow Cleanup Result), `f-force-review`(Should be Human-Reviewed) 체크박스 추가. 설정창 API Keys 탭에 E2B API Key 입력란 추가. Strictness 탭 신설(슬라이더 + L0~L3 레벨 가이드). 중복된 `<div id="statusbar">` 태그 제거 |
| js/inspector.js | v1 | `openInspector()`에 `allowCleanup`, `forceReview` 복원 로직 추가. Apply 핸들러에 `node.allowCleanup`, `node.forceReview` 저장 추가 |
| js/runner.js | v1 | `runCell()` body에 `allow_cleanup`, `force_review`, `e2b_api_key`, `hr_strictness` 백엔드 전달 추가 |
| js/settings.js | v1 | `getHRStrictness()` 함수 노출. Strictness 슬라이더 `input` 이벤트 핸들러 추가(라벨·설명 동적 업데이트). `getApiKeys()`에 `e2b` 키 추가 |
| nodes.py | v1 | `CellState`에 `allow_cleanup`, `e2b_api_key`, `force_review`, `hr_strictness`, `code_level` 필드 추가. `node_execute`에 E2B 키 유무에 따른 샌드박스/LLM 정적 분석 폴백 분기 추가. `node_classify_level` + `edge_after_classify` + `_static_analyze()` 신규 추가. `edge_after_execute` 반환값 `wrap` → `classify_level` 변경 |