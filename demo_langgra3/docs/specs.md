# 프로젝트 명세: 비주얼 AI 파이프라인 빌더 (Visual AI Pipeline Builder)

## 1. 프로젝트 개요 (Overview)

본 프로젝트는 '확률적으로 동작하는 LLM을 결정론적인 워크플로우로 제어'하기 위한 **노드 기반 비주얼 프로그래밍 플랫폼**입니다. 사용자가 자연어로 로직을 기술하면 LLM이 실행 가능한 코드를 생성하고, 이를 데이터 흐름도(Dataflow Graph) 상에서 결합하여 하나의 완성된 애플리케이션(예: 인터랙티브 계산기, 비디오 텍스트 분석기 등)을 구축하는 것을 목표로 합니다.

## 2. 프로젝트 목적 (Objectives)

* **추상화의 구체화:** 사용자의 모호한 의도를 명확한 I/O 규격(Schema)을 가진 노드 구조로 변환.
* **신뢰성 확보:** LLM의 코드 생성 오류(Black-box error)를 잡아내기 위한 유닛 테스트 및 가드레일 시스템 내장.
* **상태 관리의 시각화:** 복잡한 에이전트의 상태(State) 변화를 캔버스 위에서 실시간으로 모니터링 및 디버깅.
* **모듈화된 재사용성:** 특정 기능을 수행하는 셀(Cell)을 부품화하여 다양한 파이프라인에 즉시 이식.

---

## 3. 프론트엔드 지향점 (Frontend Philosophy)

프론트엔드는 단순한 UI를 넘어 '런타임 시뮬레이터'의 역할을 지향합니다.

* **직관적 선언 프로그래밍:** 사용자가 `if-else` 분기를 직접 그리는 대신, 병렬 노드 배치와 데이터 필터링을 통해 로직을 구성하도록 유도.
* **실시간 상태 추적 (Visual Tracing):** 데이터가 노드 사이의 엣지(Edge)를 흐르는 모습을 시각화하고, 각 단계의 스냅샷(이미지, 텍스트, JSON)을 즉각 확인.
* **하이브리드 인터페이스:** 전문가를 위한 상세 설정 패널(모델 파라미터, 시스템 프롬프트)과 일반인을 위한 단순화된 위젯(버튼, 입력창) 모드를 동시에 제공.

---

## 4. 백엔드 지향점 (Backend Philosophy)

백엔드는 '안정적인 실행 엔진'과 '엄격한 검증자'의 역할을 지향합니다.

* **LangGraph 기반 상태 제어:** Cyclic 구조를 배제한 DAG(Directed Acyclic Graph)를 기본으로 하되, Checkpointer를 통한 상태 보존 및 Human-in-the-loop(일시정지 및 개입) 지원.
* **엄격한 I/O 컨트랙트:** 모든 노드는 실행 전 입력 데이터의 유효성을 검사하며, LLM 응답을 JSON 모드 및 후처리 스니펫(Sanitizer)으로 정제하여 데이터 오염 방지.
* **자동 검증 시스템 (Auto-Validator):** 사용자가 정의한 샘플 I/O를 바탕으로, LLM이 생성한 코드가 의도대로 작동하는지 런타임 직전에 단위 테스트 수행.
* **동적 코드 실행 환경:** 생성된 Python 코드를 격리된 환경에서 안전하게 실행하고 결과를 파이프라인으로 반환.

---

## 5. 핵심 데이터 흐름 규칙 (Protocol)

1. **Strict Direction:** 모든 흐름은 `INPUT`에서 시작하여 `OUTPUT`으로 종료되며, 역방향 흐름은 금지됨.
2. **Schema-First:** 노드 간 연결 시 데이터 타입(Type)과 키 값(Key)의 일치 여부를 최우선으로 검증.
3. **Parallel Intelligence:** 하나의 입력을 여러 노드로 분사(Broadcasting)하고, 결과를 통합(Aggregating)할 때 조건에 맞는 결과만 선택하는 전략 취득.

---

### [LLM을 위한 요약 가이드]

> "이 시스템은 사용자가 '말'로 만드는 프로그램을 '구조'로 고정하는 장치다. 너는 각 셀의 I/O를 명확히 정의하고, 이전 셀에서 넘어온 데이터 스키마를 깨지 않도록 코드를 생성해야 한다. 특히 LLM 특유의 사족을 배제하고 오직 규격화된 데이터만을 반환하는 '순수 함수적 노드'를 만드는 데 집중하라."

